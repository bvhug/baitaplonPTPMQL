@model IEnumerable<Nhom2.Models.TenXeModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
    <div>
        <a asp-action="Upload">Create Excel</a>
    </div>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenXe_BienSo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenXe_BienSo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.XeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.XeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.XeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
